interface:
  display_name: "Ship Linear Release"
  short_description: "Готовит milestone и выпускает одним batch"
  default_prompt: "Используй $ship-linear-release с контролируемым числом workers: подготовь незавершённые Linear-задачи в изолированных worktrees и выпусти их одним проверенным batch без per-issue deploy."

policy:
  allow_implicit_invocation: false
